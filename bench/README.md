# Benchmark artifacts

No benchmark result exists in Phase 01.

Future runs must commit raw output and identify macOS/XNU build, XNU CUBIC baseline,
both Macs, chip and NIC models, MTU, dummynet/PF rules, timestamp source, thermal
state, trial ordering, sample count, and variance. Proxy measurements must be labeled
as proxies and cannot support cache-miss claims.
